<section class="CScrollText">
  <div class="content">
    <h1 class="heading">
      <span class="heading-text clip" data-scroll-text>「オフィス」をデザインする時代から、</span>
      <span class="heading-text clip" data-scroll-text>「はたらく」をデザインする時代へ。</span>
    </h1>
    <p class="content">
      <span class="text clip" data-scroll-text>リモートワーク、フレックスタイム制、</span>
      <span class="text clip" data-scroll-text>コワーキングスペース、サテライトオフィス…。</span>
      <span class="text clip" data-scroll-text>はたらく環境はますます多様化し、</span>
      <span class="text clip" data-scroll-text>オフィスではなくはたらき方をデザインする</span>
      <span class="text clip" data-scroll-text>『ワークデザイン』が必要とされています。</span>
      <span class="text clip" data-scroll-text>データに基づく確かな「根拠」と変動する社会に合わせた</span>
      <span class="text clip" data-scroll-text>「継続性」を駆使し、</span>
      <span class="text clip" data-scroll-text>あなたの会社だけの理想を共に実現しましょう。</span>
    </p>
  </div>
</section>

<style lang="scss">
  .CScrollText {
    height: 100vh;
    display: grid;
    place-content: center;
  }

  .heading {
    line-height: 50px;
    font-size: 24px;
    margin-bottom: 50px;

    &-text {
      display: block;
      width: max-content;
    }
  }

  .content {
    line-height: 36px;
  }

  .text {
    font-size: 16px;
    display: block;
    width: max-content;
  }

  .clip {
    color: transparent;
    -webkit-background-clip: text;
    background-clip: text;
  }
</style>

<script>
  import { gsap } from "gsap";
  class CScrollText {
    [x: string]: any;
    constructor() {
      this.scrollText = document.querySelectorAll("[data-scroll-text]");
      this.init();
    }

    updateGradient() {
      const scrollTop = window.scrollY;
      const windowHeight = window.innerHeight;
      const triggerPoint = windowHeight / 1;

      this.scrollText.forEach((item) => {
        const offsetTop = item.offsetTop;
        const offsetHeight = item.offsetHeight;
        const rectTop = offsetTop - scrollTop;
        const rectBottom = rectTop + offsetHeight;

        if (rectTop < triggerPoint && rectBottom >= 0) {
          const progress = (triggerPoint - rectTop) / triggerPoint;
          const ratio = progress * 400;

          gsap.set(item, { backgroundImage: `linear-gradient(90deg, rgb(68, 64, 63) ${ratio}%, rgba(68, 64, 63, .3) 0%)` });
        }
      });
    }

    init() {
      window.addEventListener("scroll", this.updateGradient.bind(this));
      this.updateGradient();
    }
  }

  new CScrollText();
</script>
